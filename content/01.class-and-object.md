# Classes and Objects

In OOP, a class is a blueprint for creating objects. It defines the attributes (fields/variables) and behaviors (methods) that the objects created from the class will have. An object, on the other hand, is an instance of a class.
![image 5](https://github.com/user-attachments/assets/8088858d-e914-43ce-ab20-4bda20a28d38)

The class in the image above acts as a blueprint or a template to represent the structure of the objects, while objects are the actual variables that fill the blueprint with details.

Imagine you want to represent a **Person** with details like `name` (string), `age` (integer), and `height` (float). Instead of using separate variables, you can group them together in a **class**.


## Defining a Class
To define a class in C#, use the `class` keyword, followed by the class name and a pair of curly braces `{}`. Inside the class, define its **fields (variables)** and **methods**.


```csharp
class ClassName {
    // Fields (variables)
    dataType variableName;

    // Methods
    returnType MethodName() {
        // Method body
    }
}
```
## Class Members

### Fields
Fields are variables that belong to a class. They store the **attributes** of an object.

### Methods
Methods are functions defined inside a class that perform **actions**. They can access and modify the fields of the class.

### Example
- Fields and Methods
```csharp
class Person {
    public string name;  // Field
    public int age;      // Field

    // Method
    public void SayHello() {
        Console.WriteLine("Hello, my name is " + name);
    }
}

```
## Creating an Object
Once a class is defined, you can create objects (instances) of that class using the `new` keyword. Each object has its own copy of the fields defined in the class.


```csharp
ClassName objectName = new ClassName();
```
### Example
- Creating and using Objects
```csharp
class Person {
    public string name;
    public int age;
    public float height;
}

class Program {
    static void Main(string[] args) {
        // Create an object of the Person class
        Person person1 = new Person();

        // Assign values to the object's fields
        person1.name = "Sultan";
        person1.age = 30;
        person1.height = 170.7f;

        // Print the object's details
        Console.WriteLine("Name: " + person1.name);
        Console.WriteLine("Age: " + person1.age);
        Console.WriteLine("Height: " + person1.height);
    }
}
```

Output:
```
Name: Sultan
Age: 30
Height: 170.7
```

## Constructors
A constructor is a special method used to initialize objects. It has the same name as the class and no return type. It's automatically called when an object is created.

### Types of Constructors
1. **Default Constructor**: A constructor with no parameters.  
2. **Parameterized Constructor**: A constructor that accepts parameters to initialize fields.

```csharp
class ClassName {
    // Constructor
    public ClassName() {
        // Initialization code
    }
}
```

### Example: Default Constructor
```csharp
class Person {
    public string name;
    public int age;

    // Default constructor
    public Person() {
        name = "Unknown";
        age = 0;
    }

    public void DisplayInfo() {
        Console.WriteLine("Name: " + name + ", Age: " + age);
    }
}

class Program {
    static void Main(string[] args) {
        Person person1 = new Person();  // Default constructor is called
        person1.DisplayInfo();
    }
}
```

Output:
```
Name: Unknown, Age: 0
```

### Example: Parameterized Constructor
```csharp
class Person {
    public string name;
    public int age;

    // Parameterized constructor
    public Person(string personName, int personAge) {
        name = personName;
        age = personAge;
    }

    public void DisplayInfo() {
        Console.WriteLine("Name: " + name + ", Age: " + age);
    }
}

class Program {
    static void Main(string[] args) {
        Person person1 = new Person("Sultan", 30);  // Constructor with parameters
        person1.DisplayInfo();
    }
}
```

Output:
```
Name: Sultan, Age: 30
```


## Access Modifiers
Access modifiers determine the visibility of class members (fields, methods, and constructors). They control which parts of the program can access a member.

#### Types of Access Modifiers
1. Public: Accessible from anywhere.  
2. Private: Accessible only within the same class.  
3. Protected: Accessible within the same class and derived classes.  
4. Internal: Accessible only within the same assembly.

### Example
```csharp
class Person {
    public string name;    // Public field
    private int age;       // Private field

    // Public method to access private field
    public void SetAge(int personAge) {
        age = personAge;
    }

    public int GetAge() {
        return age;
    }
}

class Program {
    static void Main(string[] args) {
        Person person1 = new Person();
        person1.name = "Sultan";  // Public field can be accessed directly
        person1.SetAge(30);       // Private field accessed via public methods

        Console.WriteLine("Name: " + person1.name);
        Console.WriteLine("Age: " + person1.GetAge());
    }
}
```

Output:
```
Name: Sultan
Age: 30
```

>[!Note]
> Always use private fields with public getter and setter methods for better encapsulation and data security.


## Practice

1. Define a class `Book` with:  
    - Fields: `Title` (string), `Author` (string), `Price` (float).  
    - A parameterized constructor to initialize these fields.  
    - A method `DisplayDetails()` to print the book details.  

2. Create two objects of the `Book` class, assign values using the constructor, and call the `DisplayDetails()` method.

Expected output:
```
Title: The Alchemist, Author: Paulo Coelho, Price: $15.99
Title: 1984, Author: George Orwell, Price: $12.49
```
